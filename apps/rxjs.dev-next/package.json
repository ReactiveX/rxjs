{
  "name": "rxjs-dev-next",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "api:generate": "tsx tools/api-generator/generate-typedoc-json.ts",
    "dev": "npm run api:generate && vitepress dev",
    "build": "npm run api:generate && vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "typedoc": "^0.26.0",
    "tsx": "^4.7.0",
    "shiki": "^1.0.0",
    "vitepress": "^1.6.4"
  }
}
