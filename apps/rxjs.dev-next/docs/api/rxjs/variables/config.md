[API](../../index.md) / [rxjs](../index.md) / config

# Variable: config

## Description

The [GlobalConfig](../interfaces/GlobalConfig.md) object for RxJS. It is used to configure things
like how to react on unhandled errors.

```ts
const config: GlobalConfig;
```

Defined in: [observable/src/observable.ts:387](https://github.com/ReactiveX/rxjs/blob/master/packages/observable/src/observable.ts#L387)

The [GlobalConfig](../interfaces/GlobalConfig.md) object for RxJS. It is used to configure things
like how to react on unhandled errors.
