[API](../../index.md) / [ajax](../index.md) / AjaxDirection

# Type Alias: AjaxDirection

## Description

Valid Ajax direction types. Prefixes the event `type` in the
[AjaxResponse](../classes/AjaxResponse.md) object with "upload*" for events related
to uploading and "download*" for events related to downloading.

```ts
type AjaxDirection = 'upload' | 'download';
```

Defined in: [rxjs/src/internal/ajax/types.ts:8](https://github.com/ReactiveX/rxjs/blob/master/packages/rxjs/src/internal/ajax/types.ts#L8)

Valid Ajax direction types. Prefixes the event `type` in the
[AjaxResponse](../classes/AjaxResponse.md) object with "upload*" for events related
to uploading and "download*" for events related to downloading.
