[API](../../index.md) / [index](../index.md) / config

# Variable: config

## Description

The [GlobalConfig](../interfaces/GlobalConfig.md) object for RxJS. It is used to configure things
like how to react on unhandled errors.

```ts
const config: GlobalConfig;
```

Defined in: [internal/config.ts:8](https://github.com/JessicaSachs/rxjs/blob/master/src/internal/config.ts#L8)

The [GlobalConfig](../interfaces/GlobalConfig.md) object for RxJS. It is used to configure things
like how to react on unhandled errors.
