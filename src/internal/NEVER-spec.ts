import { expect } from "chai";
import { NEVER } from "rxjs/internal/NEVER";

describe('NEVER', () => {
  it('should exist', () => {
    expect(NEVER).to.exist;
  });

  // TODO: marble tests here
});
