{
  "name": "RxJS",
  "version": "3.0.0-alpha",
  "description": "Reactive Extensions for modern JavaScript",
  "main": "index.js",
  "scripts": {
    "build_global": "mkdir -p dist/global && babel dist/es6/Rx3.js -o dist/global/Rx3.js --loose --modules umd",
    "build_es6": "rm -rf dist/cjs && tsc src/Rx3.ts --outDir dist/es6 --target ES6 -d",
    "build_cjs": "rm -rf dist/cjs && tsc src/Rx3.ts --module commonjs --outDir dist/cjs --target ES5 -d",
    "build_amd": "rm -rf dist/amd && tsc src/Rx3.ts --module amd --outDir dist/amd --target ES5 -d",
    "build_test": "rm -rf dist/test_cjs && tsc spec/observable/observable-spec.ts --module commonjs --outDir dist/test_cjs --target ES5 -d && node node_modules/.bin/jasmine dist/test_cjs/tests/**/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:ReactiveX/RxJS.git"
  },
  "keywords": [
    "Rx",
    "RxJS",
    "ReactiveX",
    "ReactiveExtensions",
    "Streams",
    "Observables",
    "Observable",
    "Stream",
    "ES6",
    "ES2015"
  ],
  "author": "Ben Lesh <blesh@netflix.com>",
  "license": "TBD",
  "bugs": {
    "url": "https://github.com/ReactiveX/RxJS/issues"
  },
  "homepage": "https://github.com/ReactiveX/RxJS",
  "devDependencies": {
    "benchmark": "^1.0.0",
    "benchpress": "^2.0.0-alpha.25",
    "http-server": "^0.7.5",
    "jasmine": "^2.3.1",
    "jasmine-core": "^2.2.0",
    "lodash": "^3.5.0",
    "platform": "^1.3.0",
    "protractor": "^1.7.0",
    "requirejs": "^2.1.16",
    "rx": "^2.4.3"
  }
}
